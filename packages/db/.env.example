# Postgres connection string used by Prisma CLI (migrations, generate, studio).
# Should match the API's DATABASE_URL and infra/docker-compose.yml.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/loltracker?schema=public
